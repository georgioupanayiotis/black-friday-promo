[build]
  command = "npm install && npm run build:demo && ls -la demo/dist/"
  publish = "demo"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
